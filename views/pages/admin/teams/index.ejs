<%- include("../../../layout/admin-header") %>

<main id="main" class="main">
  <%- include("../../../partials/admin/breadcrumb") %>
  <div class="mb-3 pt-2 pb-0 d-flex justify-content-between align-items-center">
    <h2>Teams</h2>
    <a href="/dashboard/teams/create" class="btn btn-primary btn-sm"
      >Add</a
    >
  </div>
  <div class="card">
    <div class="card-body">
      <!-- Table with stripped rows -->
      <table class="table datatable">
        <thead>
          <tr>
            <th>ID</th>
            <th>Name</th>
            <th>Profession</th>
            <!-- <th>testimony</th> -->
            <th>Rating</th>
            <th>Image</th>
            <th>Actions</th>
          </tr>
        </thead>
        <tbody>
          <% teams.forEach(team => { %>
          <tr>
            <td><%= team.id %></td>
            <td><%= team.personName %></td>
            <td><%= team.profession %></td>
            <td><%= team.facebookUrl %></td>
            <td><%= team.twitterUrl %></td>
            <td><%= team.instagramUrl %></td>
            <td><%= team.linkedInUrl %></td>
            <td>
              <img
                height="50"
                width="50"
                src="http://localhost:8088/assets/uploads/teams/<%= team.imageUrl %>"
              />
            </td>
            <td>
              <a href="/dashboard/teams/edit/<%= team.id %>"
                ><i class="bi bi-pencil-square text-success"></i
              ></a>
              <form
                action="/dashboard/teams/delete/<%= team.id %>"
                method="POST"
                style="display: inline"
              >
                <button
                  type="submit"
                  onclick="return confirm('Are you sure you want to delete this team?');"
                  class="btn"
                >
                  <i class="bi bi-trash3-fill text-danger"></i>
                </button>
              </form>
            </td>
          </tr>
          <% }) %>
        </tbody>
      </table>
      <!-- End Table with stripped rows -->
    </div>
  </div>
</main>

<!-- <%- include("../../../layout/admin-footer") %> -->
