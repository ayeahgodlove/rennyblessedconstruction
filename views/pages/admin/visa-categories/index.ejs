<%- include("../../../layout/admin-header") %>

<main id="main" class="main">
  <%- include("../../../partials/admin/breadcrumb") %>
  <div class="mb-3 pt-2 pb-0 d-flex justify-content-between align-items-center">
    <h2>Visa Categories</h2>
    <a href="/dashboard/visa-categories/create" class="btn btn-primary btn-sm">Add</a>
  </div>
  <div class="card">
    <div class="card-body">
      <!-- Table with stripped rows -->
      <table class="table datatable">
        <thead>
          <tr>
            <th>ID</th>
            <th>Visa Category</th>
            <th>description</th>
            <th>Actions</th>
          </tr>
        </thead>
        <tbody>
          <% visaCategories.forEach(visaCategory => { %>
          <tr>
            <td><%= visaCategory.id %></td>
            <td><%= visaCategory.visaCategoryName %></td>
            <td><%= visaCategory.description %></td>
            <td>
              <a href="/dashboard/visa-categories/edit/<%= visaCategory.id %>"
                ><i class="bi bi-pencil-square text-success"></i
              ></a>
              <form
                action="/dashboard/visa-categories/delete/<%= visaCategory.id %>"
                method="POST"
                style="display: inline"
              >
                <button
                  type="submit"
                  onclick="return confirm('Are you sure you want to delete this visaCategory?');"
                  class="btn"
                >
                  <i class="bi bi-trash3-fill text-danger"></i>
                </button>
              </form>
            </td>
          </tr>
          <% }) %>
        </tbody>
      </table>
      <!-- End Table with stripped rows -->
    </div>
  </div>
</main>

<%- include("../../../layout/admin-footer") %>
