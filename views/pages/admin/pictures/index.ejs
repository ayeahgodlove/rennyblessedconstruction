<%- include("../../../layout/admin-header") %>

<main id="main" class="main">
  <%- include("../../../partials/admin/breadcrumb") %>
  <div class="mb-3 pt-2 pb-0 d-flex justify-content-between align-items-center">
    <h2>Pictures</h2>
    <a href="/dashboard/pictures/create" class="btn btn-primary btn-sm">Add</a>
  </div>
  <div class="card">
    <div class="card-body">
      <!-- Table with stripped rows -->
      <table class="table datatable">
        <thead>
          <tr>
            <th>ID</th>
            <th>Title</th>
            <th>Picture</th>
            <!-- <th>Icon</th> -->
            <th>Actions</th>
          </tr>
        </thead>
        <tbody>
          <% pictures.forEach(picture => { %>
          <tr>
            <td><%= picture.id %></td>
            <td><%= picture.title %></td>
            <td>
              <img
                height="50"
                width="50"
                src="http://localhost:8088/uploads/pictures/<%= picture.imageUrl %>"
              />
            </td>
            <!-- <td><i class="<%= picture.icon %> fa-2x"></i></td> -->
            <td>
              <a href="/dashboard/pictures/edit/<%= picture.id %>"
                ><i class="bi bi-pencil-square text-success"></i
              ></a>
              <form
                action="/dashboard/pictures/delete/<%= picture.id %>"
                method="POST"
                style="display: inline"
              >
                <button
                  type="submit"
                  onclick="return confirm('Are you sure you want to delete this picture?');"
                  class="btn"
                >
                  <i class="bi bi-trash3-fill text-danger"></i>
                </button>
              </form>
            </td>
          </tr>
          <% }) %>
        </tbody>
      </table>
      <!-- End Table with stripped rows -->
    </div>
  </div>
</main>

<%- include("../../../layout/admin-footer") %>
